1. hashed  2. id, org_id, token_hash, label, device_note, created_by,                                                                                                                                                                                         
  created_at, revoked_at, revoked_by, last_claimed_at Hash the token. Store hash, return plaintext URL once at creation, never again. 3. One anon user per (device, org). PK = auth_user_id. Columns: auth_user_id, org_id, session_id, claimed_at.             
  On session revoke: DELETE FROM kitchen_claims WHERE session_id = revoked. On re-claim with new token: same anon user, claim row recreated via ON CONFLICT (auth_user_id) DO UPDATE. No new anon user minted. 4. Kitchen-only invisible. Office sees           
  everything.                                                                                                                                                                                                                                                     Columns added: is_cleared bool default false, cleared_at timestamptz. Skip cleared_by_session_id — overkill for v1.                                                                                                                                             Kitchen client filters is_cleared = false in the SELECT. Kitchen UPDATE grant includes is_cleared so kitchen can both clear and un-clear. RLS doesn't gate on the flag. 5.Client-side .select() column omit in EventsBanquetsPage.jsx. No view, no RLS        
  column rule. Within-org column exposure is a product call, not a security one. 6.(a) /k/:slug/claim/:token — first-visit page, claims and redirects to /k/:slug.                                                                                              
  Cleanest. Token isn't sitting in the URL bar after claim. Query params (b) get logged in browser history, server logs, referer headers. Form (c) adds friction with no benefit. 7. Confirm. Supabase persists in localStorage by default. Tablet stays        
  linked across reboots until revoke or storage clear. Re-claim on every load would be unusable. 8.Acceptable. Up to ~1hr lag on revoke is fine for a kitchen tool.                                                                                             
  Forced disconnect needs a realtime channel + client-side kitchen_claims watcher. Real cost in code and connection overhead. Not worth it for v1. Revisit only if a real incident forces it. 9. Confirm. The Phase 7.5 verification migration (or a one-shot   
  SQL block in the test plan) generates a fresh kitchen_sessions row for test-org, you copy the plaintext URL once, that's the new dev URL. Old /k/test-org raw-anon-key access stops working — by design. 10. Confirm 11. enabled 12. Confirmed: anon branch   
  above, returns after stamping. Do NOT interleave 13.[Pasted text #1 +22 lines] 14.[Pasted text #2 +7 lines](b) Drops kitchen-links as a standalone /o/:orgSlug/kitchen-links route with a temporary link in the user menu, defers the Settings dropdown to a  
  later phase. 15. [Pasted text #5 +8 lines] 16.Single file. ~250 lines is fine.Single file. ~250 lines is fine       